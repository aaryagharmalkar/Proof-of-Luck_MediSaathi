# MediSaathi Backend – copy to .env and fill in values.
# Do not commit .env (it is gitignored).

# ============== Required ==============
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
JWT_SECRET=your-jwt-secret

# Security (required)
SECRET_KEY=your-secret-key
REPORT_ENCRYPTION_KEY=your-32-byte-hex-or-base64-key

# ============== Server ==============
HOST=0.0.0.0
PORT=5050
DEBUG=false
ENV_NAME=production

# CORS: comma-separated allowed origins (e.g. https://app.example.com)
CORS_ORIGINS=https://your-frontend.com,http://localhost:5173

# ============== API ==============
API_V1_PREFIX=/api/v1
PROJECT_NAME=MediSaathi API

# ============== AI (optional; leave empty to disable features) ==============
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=mistral
OLLAMA_EMBED_MODEL=nomic-embed-text
